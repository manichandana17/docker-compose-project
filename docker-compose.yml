version: '3'

services:
  web:
    build: .
    ports:
      - "8099:80"
    depends_on:
      - db

  db:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: root123
    ports:
      - "3307:3306"